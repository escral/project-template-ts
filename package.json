{
    "name": "@escral/package-template",
    "version": "0.0.0",
    "license": "MIT",
    "type": "module",
    "exports": {
        ".": "./dist/index.js"
    },
    "types": "./dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "pub": "yarn build && npx new-version -s && npm publish",
        "build": "vite build",
        "test": "vitest",
        "test:types": "vitest --typecheck.only"
    },
    "devDependencies": {
        "@escral/lint": "^2.0",
        "@escral/versioning": "^1.5",
        "@types/node": "^22.10",
        "tsdown": "^0.15.12",
        "typescript": "^5.9.3",
        "vite": "^7.1.9",
        "vite-plugin-dts": "^4.3",
        "vitest": "^3.2.4"
    },
    "engines": {
        "node": ">=18.0"
    }
}
